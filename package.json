{
  "name": "grid-config",
  "version": "1.0.0",
  "description": "GRID Config — PC Configurator",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
